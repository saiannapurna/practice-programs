#include <stdio.h>
#include<string.h>
int main()
{       int i,cnt=0;
int len1,len2;
    char s[30];
    char a[8];
    gets(s);
    gets(a);
    len1=strlen(s);
    len2=strlen(a);
        for(i=0;i<len2;i++)
        {
            s[len1+i]=a[i];
        }
        printf("%s",s);
    return 0;
}
